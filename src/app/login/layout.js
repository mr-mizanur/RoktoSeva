export const metadata = {
  title: "Login",
  description:
    "Sign in to your RoktoSeva account to manage blood donation requests, track donors, and coordinate emergency blood needs across Bangladesh.",
  robots: { index: false, follow: false },
};

export default function LoginLayout({ children }) {
  return children;
}
