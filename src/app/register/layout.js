export const metadata = {
  title: "Register",
  description:
    "Create a free RoktoSeva account. Register as a blood donor, set your blood group and location, and start saving lives across Bangladesh.",
  robots: { index: false, follow: false },
};

export default function RegisterLayout({ children }) {
  return children;
}
